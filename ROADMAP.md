# ROADMAP.md

只记录到当前进行中的阶段；新阶段在前一阶段验收发布后再补。

## 第 0 阶段：协作准备 ✅

- 交付：协作文档（CLAUDE / ARCHITECTURE / ROADMAP）、空 workspace 骨架、CI、连接远端并首推。
- 验收：本地 `cargo build` 通过；GitHub Actions 绿勾。

## 第 1 阶段：最小 demo ✅（已发布 `v0.1.0`）

- 目标：可玩的双人本地对战。
- 范围：
  - `pong-core`：球场状态、挡板移动、球的运动与反弹（墙 / 挡板）、失分与计分、先到 11 分获胜。
  - 反弹物理取最简档：固定 45° 反射、发球方向随机；击球位置变角与球速加速留待后续阶段。
  - `pong-tui`：终端渲染球场与比分、键盘（左 W/S，右 ↑/↓）、胜利画面、R 重开、q 退出。
- 明确不做：AI 对手、暂停、菜单、联机。
- 验收：真人完整对局一局至 11 分，无崩溃、无花屏。
- 发布：验收后打 tag `v0.1.0` 并创建 GitHub Release。

## 第 2 阶段：手感优化（进行中）

- 目标：不加新功能，打磨游玩手感与体验。
- 范围：
  - 场地：球场加长为 140×60（原 100×60），横穿时间约 2.5 秒，给足反应时间。
  - 输入：不依赖任何键盘扩展协议（kitty 键盘协议在 VTE 系终端不可用，首测 Ptyxis 即报错），改用自动重复推断：按住的键经系统自动重复（约 30 次/秒）持续上报事件，事件流中断即判定松开：检测窗自适应于实测重复间隔（确认后间隔的滑动平均 ×2.5，夹在 60–150ms；典型 30Hz 流约 75ms 检出，首个 500ms 重复延迟不进样本），首个重复到达前的等待窗为 600ms（需盖过系统重复延迟）。**三级速度（平滑过渡）**：确认前以固定 30 单位/秒爬行（轻点行程约 0.3 场高），确认后经线性斜坡约 0.25 秒平滑加速至全速 80 单位/秒（与球速持平）；反向保持当前速度；若终端报告真实松开事件则立即生效。
  - 节奏：失分后停顿 1 秒再发球，球飞向刚丢分的一方；停顿期间屏上显示发球指示。发球角度从 {0°, ±15°, ±30°, ±45°} 随机选取（平射双倍权重，均不超 45°；沿用作者 FPGA 版 Pong-dld 的查表设计）。
  - 音效：击拍与回合结束发 440Hz 短"嘀"，终局发更高音高 880Hz；事件经快照流向前端（后端零 I/O，仅播报 `GameEvent`），无音频设备时回退终端铃声。
  - 物理：反弹角随击球位置变化（拍沿陡、拍心平），上限约 60°；**反弹只改方向，合速度大小恒为 BALL_SPEED**。
  - 视觉：得分瞬间比分闪烁；发球停顿显示倒计时；球改为实心方块字符（终端中圆形字符逐格跳变，方块更平滑）。明确不做：球尾迹、击拍闪光、每拍加速。
- 验收：双人完整对局一局，确认球场比例与反应时间、松手在约 0.15 秒内停（自动重复推断）、发球停顿可预期、反弹角随击球点变化且球速不变、得分闪烁、方形球。
- 发布：验收后打 tag `v0.2.0` 并创建 GitHub Release。

## 后续

第 2 阶段发布后再规划。
