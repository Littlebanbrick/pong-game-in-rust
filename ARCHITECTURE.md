# ARCHITECTURE.md

前后端架构契约。涉边界的改动，先改本文件，再动代码。

## 布局

| crate | 角色 | 职责 | 绝对禁止 |
|---|---|---|---|
| `pong-core` | 后端 | 游戏状态、物理、规则、胜负判定 | 终端 I/O、依赖 ratatui/crossterm |
| `pong-tui` | 前端 | ratatui 渲染、键盘输入、帧调度 | 游戏规则、物理计算 |

## 通信模型：后端权威 + 消息传递

后端在独立线程中以固定时间步（60 TPS）权威推进全部游戏逻辑；前端不持有任何游戏状态，双方只通过两条通道通信：

```
按键 → pong-tui 主线程 --InputEvent--> pong-core 游戏线程
画面 ← pong-tui 渲染   <--GameSnapshot-- （60 TPS 物理推进）
```

- **命令通道**（前端 → 后端）：`InputEvent`（按键、重开、退出）。
- **状态通道**（后端 → 前端）：`GameSnapshot`，每 tick 一份的完整球场状态。
- 消息类型定义在 `pong-core`（后端定义协议，前端只消费），第一阶段落地。
- 前端拿不到后端内部可变状态——边界是物理的，不是靠自觉。

## 坐标映射

核心坐标系（140×60，第二阶段起加长以留足反应时间）是抽象的，后端对屏幕尺寸一无所知。终端字符格约为 1:2（宽:高），前端按 ~14:3 的列行比布置球场，使场地在屏幕上呈 ~7:3 横向矩形、球的轨迹角度在屏幕上不失真。

## 关停

前端按 q 时发送 `Shutdown`，后端线程优雅退出，进程收尾。

## 输入（第二阶段起）

终端普遍不报告松键事件；kitty 键盘协议能解决但 VTE 系（GNOME Terminal、Ptyxis 等）不支持，故前端不依赖任何键盘扩展协议，改用**自动重复推断**：按住的键经系统自动重复（约 30 次/秒）持续产生事件，事件流中断超过阈值即判定松开。传统终端把重复按键当作全新按下事件送达，因此"重复中"的判定依据是同键事件再次到达，而非事件类型。

- 阈值（`pong-tui/src/input.rs`）：重复流建立后松开检测窗**自适应**于实测重复间隔——确认后到达的事件间隔经滑动平均（权重 0.5），窗 = 间隔 × 2.5，夹在 60–150ms；典型 30Hz 流约 75ms 判定松开，尚无样本时回退 150ms。首个重复到达前以 `HOLD_START_TIMEOUT` = 600ms 兜底（需大于系统重复延迟，常见默认约 500ms，且该延迟不进样本以防毒化估计）。
- **三级速度（平滑过渡）**：重复流到达即"确认按住"，`SetPaddleDirection` 事件携带 `held` 标志。确认前以 `PADDLE_TAP_SPEED`（固定 30，与全速解耦——它是盲窗过冲的保险，预算不随全速变化）恒速爬行；确认后经 `PADDLE_ACCEL` 线性斜坡在约 0.25 秒内平滑加速至 `PADDLE_SPEED`（80，与球速持平）；松开立即归零；反向保持当前速度直接掉头。轻点在确认窗内的行程约为场高 0.3。
- 同侧两键同按，后按者生效；一键过期后回退到仍按住的键（连同其确认状态）；若终端报告真实松开事件（如 kitty 协议）则立即生效，无需等待超时。
- 已知代价：系统关闭按键重复时，"按住"永远无法确认，退化为 1/3 速度定长点动。

## 状态机（第二阶段范围）

`Serving { toward, ticks_left } → Playing →（失分）→ Serving → … → GameOver { winner } →（R 重开）→ Serving`；q 随时退出。

- 发球规则：失分后停顿 1 秒，球飞向刚丢分的一方；开局随机方向。发球角度从 {0°, ±15°, ±30°, ±45°} 查表随机，平射双倍权重（沿用 FPGA 版 Pong-dld 的表形），均不超 45°。
- 物理约束：反弹只改方向；球的合速度大小恒为 `BALL_SPEED`，反弹角随击球位置变化（拍沿最陡 60°，拍心平回）。
