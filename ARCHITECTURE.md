# ARCHITECTURE.md

前后端架构契约。涉边界的改动，先改本文件，再动代码。

## 布局

| crate | 角色 | 职责 | 绝对禁止 |
|---|---|---|---|
| `pong-core` | 后端 | 游戏状态、物理、规则、胜负判定 | 终端 I/O、依赖 ratatui/crossterm |
| `pong-tui` | 前端 | ratatui 渲染、键盘输入、帧调度 | 游戏规则、物理计算 |

## 通信模型：后端权威 + 消息传递

后端在独立线程中以固定时间步（60 TPS）权威推进全部游戏逻辑；前端不持有任何游戏状态，双方只通过两条通道通信：

```
按键 → pong-tui 主线程 --InputEvent--> pong-core 游戏线程
画面 ← pong-tui 渲染   <--GameSnapshot-- （60 TPS 物理推进）
```

- **命令通道**（前端 → 后端）：`InputEvent`（按键、重开、退出）。
- **状态通道**（后端 → 前端）：`GameSnapshot`，每 tick 一份的完整球场状态。
- 消息类型定义在 `pong-core`（后端定义协议，前端只消费），第一阶段落地。
- 前端拿不到后端内部可变状态——边界是物理的，不是靠自觉。

## 坐标映射

核心坐标系（100×60）是抽象的，后端对屏幕尺寸一无所知。终端字符格约为 1:2（宽:高），前端按 ~10:3 的列行比布置球场，使场地在屏幕上呈 ~5:3 横向矩形、45° 球轨迹在屏幕上仍呈 45°。

## 关停

前端按 q 时发送 `Shutdown`，后端线程优雅退出，进程收尾。

## 状态机（第一阶段范围）

`Playing → GameOver { winner } → Playing（R 重开）`；q 随时退出。
